import logging
import config
import logging
import os
import matplotlib.pyplot as plt


def plotting_data(data_points):
    plt.axis([])


if __name__ == '__main__':
    logging.basicConfig(level=logging.INFO, format='%(asctime)s:%(levelname)s:%(message)s')
